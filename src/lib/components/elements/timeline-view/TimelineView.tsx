import styles from "./TimelineView.module.scss";

export interface TimelineViewProps { }

export const TimelineView = ({

}: TimelineViewProps) => {
  // time columns, items rows
  return (
    <div className={styles.wrapper}>

    </div>
  );
};
