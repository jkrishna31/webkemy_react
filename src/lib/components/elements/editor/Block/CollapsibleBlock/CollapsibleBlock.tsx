export interface CollapsibleBlockProps {
  data?: any;
}

const CollapsibleBlock = ({
  data,
}: CollapsibleBlockProps) => {
  return (
    <div>{"CollapsibleBlock"}</div>
  );
};

export default CollapsibleBlock;
