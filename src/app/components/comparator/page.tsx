import React from "react";

import { PageSetup } from "@/components/managers";

const Page = () => {
  return (
    <main>
      <PageSetup pageKey="comparator" />
    </main>
  );
};

export default Page;
