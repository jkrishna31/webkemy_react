import React from "react";

import { PageSetup } from "@/components/managers";

const Page = () => {
  return (
    <main>
      <PageSetup pageKey="data-table" />
    </main>
  );
};

export default Page;
